{
  "name": "songcraft-mermaid-server",
  "version": "1.0.0",
  "description": "Local Mermaid diagram server for Songcraft project",
  "type": "module",
  "workspaces": [
    "songcraft",
    "songcraft-api",
    "shared"
  ],
  "scripts": {
    "start": "npm run build && node server.js",
    "dev": "node dev.js",
    "build": "node build-diagrams-simple.js",
    "build-puppeteer": "node build-diagrams.js",
    "watch": "node watch-diagrams.js",
    "server": "node server.js",
    "dev:api": "DATABASE_URL=\"postgresql://songcraft:songcraft_dev_password@localhost:5432/songcraft\" npm run dev --workspace=songcraft-api",
    "dev:frontend": "VITE_API_URL=\"http://localhost:4500\" npm run dev --workspace=songcraft",
    "dev:db": "docker compose up -d db"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "express": "^4.19.2",
    "mermaid": "^10.9.1"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.9.0"
  }
}
