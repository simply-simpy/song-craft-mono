# Production environment example
# Copy to .env.prod and fill in real values. Do NOT commit secrets.

# Postgres
POSTGRES_USER=songcraft
POSTGRES_PASSWORD=change_me
# Use a distinct DB for production
POSTGRES_DB=songcraft_prod

# Backend API
# You can construct DATABASE_URL from the above or define it explicitly
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# Auth (example)
CLERK_SECRET_KEY={{CLERK_SECRET_KEY}}

# Frontend
# Inside Docker, services talk over the Compose network
VITE_API_URL=http://backend:4500
# Optional app metadata
VITE_APP_TITLE=Songcraft

